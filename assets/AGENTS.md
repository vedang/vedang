## Lessons

- README maintainability improves a lot when Markdown handles layout and HTML is limited to the few cases GitHub Markdown doesn’t cover well.
- SVG complexity often hides in decorative substructures; removing those gives a large simplification payoff with little visible loss.
- Separating “create the design” and “simplify the implementation” into different commits makes documentation history much easier to understand.
